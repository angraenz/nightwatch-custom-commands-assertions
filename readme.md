# Custom https://nightwatchjs.org/ commands and assertions

add below dependencies to your `package.json` file

```
{
    "devDependencies" : {
        "nightwatch-custom-commands-assertions" : "angraenz/nightwatch-custom-commands-assertions#v1.0.0"
    }
}
```

then run

```
yarn install
```